services:
  - type: web
    name: seat-booking-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    disk:
      name: db
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: DB_PATH
        value: /data/seats.db
